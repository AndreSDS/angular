import { PipeImpuraPipe } from './pipe-impura.pipe';

describe('PipeImpuraPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeImpuraPipe();
    expect(pipe).toBeTruthy();
  });
});
